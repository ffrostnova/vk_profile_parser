# VK Profile Search Bot

Структурированный Telegram бот для поиска пользователей ВКонтакте по ключевым словам.

## 📋 Описание

Бот позволяет искать пользователей ВКонтакте по заданным критериям:
- Поиск по городам
- Фильтрация по ключевым словам в профиле и на стене
- Фильтрация по возрасту и полу
- Ротация VK токенов для избежания блокировок
- Экспорт результатов в Excel

## 📁 Структура проекта

```
test_project/
├── main.py                 # Точка входа, запуск бота
├── config.py              # Конфигурация и константы (создается из config.example.py)
├── config.example.py      # Пример конфигурации
├── utils.py               # Вспомогательные функции
├── vk_api_manager.py      # Менеджер VK API
├── storage.py             # Работа с хранилищем данных
├── search_engine.py       # Движок поиска
├── search_runner.py       # Запуск и управление поиском
├── handlers/             # Обработчики команд
│   ├── __init__.py
│   ├── commands.py        # Обработчики команд
│   ├── decorators.py      # Декораторы
│   ├── keyboard.py        # Клавиатуры
│   └── notifications.py   # Уведомления
├── requirements.txt       # Зависимости
├── .gitignore            # Игнорируемые файлы
└── README.md             # Документация
```

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository_url>
cd test_project
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

или

```bash
pip3 install -r requirements.txt
```

### 3. Настройка конфигурации

Скопируйте пример конфигурации и заполните своими данными:

```bash
cp config.example.py config.py
```

Откройте `config.py` и заполните:

- **VK_TOKENS** - добавьте ваши VK токены (можно использовать несколько для ротации)
- **TELEGRAM_BOT_TOKEN** - токен вашего Telegram бота (получите у [@BotFather](https://t.me/BotFather))
- **ALLOWED_USERS** - список разрешенных пользователей (юзернеймы без @)

### 4. Запуск бота

```bash
python main.py
```

или

```bash
python3 main.py
```

## 📚 Основные модули

- **config.py** - Все константы и настройки
- **vk_api_manager.py** - Управление VK API сессиями и ротация токенов
- **storage.py** - Работа с JSON и Excel файлами
- **search_engine.py** - Логика поиска пользователей
- **search_runner.py** - Оркестрация процесса поиска
- **handlers/** - Обработчики команд и сообщений

## ⚙️ Функциональность

- ✅ Поиск пользователей ВКонтакте по любым городам и ключевым словам
- ✅ Ротация VK токенов для избежания блокировок
- ✅ Сохранение результатов в JSON и Excel
- ✅ Фильтрация по возрасту, полу, наличию фото
- ✅ Проверка стены пользователя на ключевые слова
- ✅ Статистика найденных пользователей
- ✅ Система контроля доступа по юзернеймам
- ✅ Автоматический поиск ID городов через VK API

## 🔧 Использование

1. Запустите бота командой `/start`
2. Добавьте города через кнопку "📍 Добавить города"
3. Добавьте ключевые слова через "🔍 Добавить ключевые слова"
4. Настройте возрастной диапазон через "🎯 Настройка возраста"
5. Запустите поиск кнопкой "🔎 Поиск"
6. Бот будет отправлять найденных пользователей в реальном времени
7. Скачайте результаты через "📥 Выгрузить Excel"

## ⚠️ Важно

- **Не коммитьте `config.py` в репозиторий!** В нем содержатся токены и секретные данные
- Используйте `config.example.py` как шаблон
- Рекомендуется использовать несколько VK токенов для ротации
- Соблюдайте лимиты VK API, чтобы избежать блокировок

## 📝 Лицензия

Этот проект предоставляется "как есть" без каких-либо гарантий.

## 🤝 Вклад

Приветствуются pull requests и issues для улучшения проекта.

